<wxs src="../../filter/filter.wxs" module="filter" />
<van-toast id="van-toast" />
<view class="container">

  <view class='list'>
    <navigator hover-class="none" url='../_detail/_detail?_detailId={{item.id}}' class='item' wx:for="{{_list}}" wx:key = "{{id}}" >
      
    </navigator>
  </view>
  <view class="no-list" wx:if="{{!_list.length}}">
    <image src="../../images/nothing.png" />
  </view>
</view>